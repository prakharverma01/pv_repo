'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});
exports.default = buildSelector;

var _filter = require('./filter.js');

function buildSelector() {
    var stateResolver = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : function (state) {
        return state;
    };

    return function mapStateToProps(state) {

        var result = {
            collection: (0, _filter.collection)(stateResolver(state)),
            optionGroups: stateResolver(state).optionGroups,
            appliedFilters: stateResolver(state).appliedFilters,
            keyword: stateResolver(state).keywordSearch,
            sortFn: stateResolver(state).sortFn,
            currentPage: stateResolver(state).page
        };

        return result;
    };
}